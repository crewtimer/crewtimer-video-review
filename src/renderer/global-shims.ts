// eslint-disable-next-line @typescript-eslint/no-explicit-any
export const fs = (window as unknown as any).require('fs');
// eslint-disable-next-line import/prefer-default-export
export const NOOP = 0;
